# Main Docker Compose file
# Includes all services: Langfuse, ClickHouse MCP, LibreChat, and the Admin Panel

include:
  - langfuse-compose.yml
  - clickhouse-mcp-compose.yml
  - librechat-compose.yml
  - admin-panel-compose.yml
